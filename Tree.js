class TreeNode {
    constructor(value) {
        this.value = value;  
        this.left = null; 
        this.right = null;
    }
}

class BinaryTree {
    constructor(){
        this .root=null;
    }
    insert(data,key){
        //  it creates a new node to be inserted and calls insertNode()
        var newNode=new TreeNode(data);
        if(this.root===null)
        this.root=newNode;
        else
        if(key.left==null){

        }
       
    }
}